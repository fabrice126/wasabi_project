#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sparqljs/bin/sparql-to-json" "$@"
  ret=$?
else 
  node  "$basedir/../sparqljs/bin/sparql-to-json" "$@"
  ret=$?
fi
exit $ret
